# 复制为 .env 后填入实际值。
# .env 已在 .gitignore，不会被推到仓库。

# ─── AI（V1.1 启用）───
DEEPSEEK_API_KEY=

# ─── Binance（V1 不需要，仅读 K 线公开数据）───
BINANCE_API_KEY=
BINANCE_API_SECRET=

# ─── 运行时覆盖（可选）───
# 这些会覆盖 app.yaml 中同名配置
# MM_LOGGING_LEVEL=DEBUG
# MM_SERVER_REST_PORT=8901
