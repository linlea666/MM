# MM 部署环境变量模板
# 使用：cp deploy/.env.example deploy/.env && vim deploy/.env
# 注意：.env 不入 Git（.gitignore 已忽略 deploy/.env）

# ─── AI（V1 可留空；V1.1 启用观察模式需要） ───
DEEPSEEK_API_KEY=

# ─── 宿主端口（可选覆盖，默认 8900 / 8901） ───
# 端口占用时改这里即可，无需改 compose
# MM_FRONTEND_PORT=8900
# MM_BACKEND_PORT=8901
